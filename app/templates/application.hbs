<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Blab Report Verification</a>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li>
          {{#link-to 'reports.index'}}
            {{fa-icon "sticky-note-o"}} Reports
          {{/link-to}}
        </li>

        <li>
          {{#link-to 'members.index'}}
            {{fa-icon "users"}} Members
          {{/link-to}}
        </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li>
          {{#link-to 'login'}}
            {{fa-icon "key"}} Login
          {{/link-to}}
        </li>
      </ul>
    </div>

  </div>
</nav>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      {{ outlet }}
    </div>
  </div>
</div>